from selenium.webdriver.common.by import By

class MainPageLocators(object):
    GO_BUTTON = (By.ID, "submit") #we wanna access the boton go on the search field of the python website, it has ID submit


class SearchResultsPageLocators(object):
    pass